services:
  - type: web
    name: rubby-chatbot-api
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn chatbot_api:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        value: false
